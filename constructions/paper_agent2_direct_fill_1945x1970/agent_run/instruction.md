Computational/local-search extension mode grounded in the paper construction. Treat grid.csv as the initial candidate coloring and parallelogram.csv as the domain geometry; do not use current neural checkpoint artifacts as seeds. Build or reuse a lightweight parser that converts the CSV rows into an indexed 1945-by-1970-style array with coordinates and colors. Design small, reproducible local searches around the paper construction: recolor bounded patches near detected same-color distance-1 risks, move color boundaries by one or a few grid steps, try local color swaps, test coarsened subgrids before full-resolution runs, and optionally tune v1/v2 by tiny perturbations while keeping the coloring inherited from the paper grid. Use fixed random seeds, short checkpoints, and an explicit objective such as fewer same-color unit-distance conflicts, larger safety margins, smaller exceptional-color footprint, or simpler boundary complexity. Keep all outputs independent of neural artifacts: candidate CSV diffs, parameter logs, and verifier summaries. Report both improvements and negative/no-improvement evidence, including failed move classes, search settings that stagnate, and examples where local changes create new conflicts.
